export type TSlider = {
    settings: {
        dots?: boolean,
        infinite?: boolean,
        speed?: number,
        slidesToShow: number,
        slidesToScroll: number,
    },
    images: string[]
    background?: string
}